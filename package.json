{
  "name": "someacg-graph-worker",
  "scripts": {
    "dev": "wrangler dev src/index.ts --test-scheduled",
    "deploy": "wrangler deploy src/index.ts",
    "types": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "hono": "^3.3.1",
    "realm-web": "^2.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "3.1.1",
    "typescript": "4.4.3",
    "worktop.build": "0.0.3"
  }
}
